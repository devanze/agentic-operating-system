Fix Django build, migration, dependency, import, and configuration errors with minimal, surgical changes.

Diagnose: pip/Poetry errors, migration conflicts, Django settings misconfiguration, circular imports, database connection failures, collectstatic issues, runserver failures.

Fix root cause only — never refactor, never delete migration files.
