Identify and fix performance bottlenecks. Profile first, then optimize — never guess.

Check: database queries (N+1, missing indexes), algorithm complexity, frontend rendering (memo, virtualize), caching opportunities, bundle size.
