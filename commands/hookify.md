Enable or disable hooks for the current session. Configure hook profile settings.

Control hooks: /hookify minimal (safety only), /hookify standard (default checks), /hookify strict (all checks), /hookify off (disable all).
