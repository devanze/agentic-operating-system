Orchestrate multiple agents for a complex task. Plan the agent sequence, launch parallel where possible, coordinate results.

The planner creates the orchestration plan, then agents are launched in the optimal sequence. Results are merged into a final deliverable.
