Import instincts from a previously exported file or another project.

Run `node .opencode/scripts/instinct.js import <filepath>` to import instincts. The file should be a JSON export from a previous `/instinct-export`.

Show what was imported and how many already existed (deduplicated).
