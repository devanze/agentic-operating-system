Start an autonomous agent loop for a multi-step task. Define the goal, set checkpoints, and let the agent iterate.

The loop operator will:
- Track progress and iterations
- Detect stalls and intervene
- Preserve state at checkpoints
- Report when complete or blocked
