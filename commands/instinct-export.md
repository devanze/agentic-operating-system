Export current project instincts to a shareable JSON file.

Run `node .opencode/scripts/instinct.js export [filename]` to export all instincts and session data for this project.

The exported file can be imported into another project with `/instinct-import` or shared with team members.
