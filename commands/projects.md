List all known projects with their instinct stats.

Run `node .opencode/scripts/instinct.js status` and show project-level information:

- Project name and path
- Instinct count per project
- Session count
- Last session date
- Global instinct count (promoted)

This gives an overview of knowledge accumulated across all projects.
