Show the current state of learned instincts. Run `node .opencode/scripts/instinct.js status` and display:

- Total instincts recorded for this project
- Global (promoted) instincts
- Breakdown by category
- Recent instincts (last 10)
- Recent sessions (last 5)
- Clustered instincts ready for evolution

Format the output clearly with categories and counts.
