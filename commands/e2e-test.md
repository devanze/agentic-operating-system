Run automated end-to-end tests in the browser using Playwright. Test critical user flows, form submissions, and navigation.

Uses the Playwright MCP server to automate browser interactions and verify UI behavior.
