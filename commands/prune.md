Remove stale or unnecessary context, files, and cruft. Clean up the workspace, remove temporary files, prune outdated branches.

Agent identifies what's safe to remove: temp files, old branches, stale cache, unused dependencies.
