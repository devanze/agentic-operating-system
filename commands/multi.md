Plan and execute a multi-agent workflow splitting independent tasks across parallel agents.

Identify sub-tasks that can run in parallel, assign each to appropriate specialist agent, collect and merge results into unified output.
