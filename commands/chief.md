Triage all communication channels — email, Slack, LINE, Messenger, and calendar — through a unified pipeline.

Classify messages into 4 tiers: skip (auto-archive), info_only (summary), meeting_info (calendar cross-reference), and action_required (generate draft reply).

Enforce post-send follow-through: calendar updates, relationship notes, todo management, and pending response tracking.
