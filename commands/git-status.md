Generate and review the current git status, branches, commits, and file changes.

Uses git-summary tool to provide a complete picture of current git state including staged changes, recent history, and branch diff.
