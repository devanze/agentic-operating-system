Fix Go build and compilation errors with minimal targeted changes.

Rules: read error, find root cause, apply smallest fix. Common fixes: undefined identifiers, type mismatches, missing imports, unused variables, go.mod issues, vet warnings.
