Design feature architecture by analyzing existing codebase patterns and conventions. Produce an implementation blueprint with concrete files, interfaces, data flow, and build order.

Process: pattern analysis → architecture design → implementation blueprint → build sequence (types → core → integration → UI → tests → docs).
